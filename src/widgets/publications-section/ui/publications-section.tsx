import { Container } from "@/shared/ui/container";

export function PublicationsSection() {
  return (
    <section className="border-b border-dashed border-zinc-300">
      <Container className="flex h-72 items-center justify-center text-sm text-zinc-400">
        [ widgets/publications-section ] — «Публикации»: фильтры (Новости / Интервью / Misc / Компании) + сетка карточек
      </Container>
    </section>
  );
}
